to-do-list

retornar o status code das execuções
verificar pq não lê variavel de ambiente
verificar pq ele cria o outfile e lugares/pastas diferentes
rodar e passar nos tester
checar o wait

Checar:
- The program takes 4 arguments, no more, no less (except for bonus part)
and only in the required order.
- Error management is correct: (un)existing files, files rights,
(un)existing command binary, and so forth.
- checar awk command